// actions
import { actions, actionNames } from "../actions/optimize";

// reducers
import { reducers, selectors } from "../reducers/optimize";

// thunks
import { thunks } from "../thunks/optimize";

export const Optimize = {
	actions,
	actionNames,
	reducers,
	selectors,
	thunks,
};
