// actions
import { actions, actionNames } from "../actions/review";

// reducers
import { reducers, selectors } from "../reducers/review";

// thunks
import { thunks } from "../thunks/review";

export const Review = {
	actions,
	actionNames,
	reducers,
	selectors,
	thunks,
};
