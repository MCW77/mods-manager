// actions
import { actions, actionNames } from "../actions/settings";

// reducers
import { reducers, selectors } from "../reducers/settings";

export const Settings = {
	actions,
	actionNames,
	reducers,
	selectors,
};
